---
trigger: always_on
---

Styling: Strict Tailwind CSS (utilize utility classes efficiently).
Component Library: Shadcn UI. Assume the components (like Button, Card, Input) are already installed in the @/components/ui folder.
Icons: Use Lucide React for icons.